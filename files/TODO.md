# Where I left off

# Overall plan
- [x] Add root user to fedora workstations.
- [x] Change ansible_user to root so I can connect without a sudo password.
- [x] Figure out a way of adding users & groups automatically.
- [x] Set up group user addition
- [x] Set up ansible pull configuration for clients
    - With clients, it should simply be necessary use an ansible ad-hoc command to execute the ansible-pull command, which should set everything up.
    - There should be one configuration for staff workstations and one for pupils.
- [x] Set up Veyon on pupil workstations
- [x] Set up home directories hosted on nfs server
- [x] Set home directories to automount for each user
- [x] Set home directory for each user as mounted share
- [x] Ensure the setbool thing
- [x] Check the initial method with the kerberos key
- [x] Set up /etc/skel for pupils in the nfs server at the location */opt/templates/pupil_skel.tar.gz*
- [x] Prepare ansible pull config for staff workstations.
- [x] Set up /etc/skel for staff in the nfs server at the location */opt/templates/staff_skel.tar.gz*
- [x] Uncomment section in provision_user_homes.yml when finished with the staff workstation
- [x] Create policies.json file for firefox.
- [ ] Create base image of Debian workstation (set hostname, enable root user, enable ssh root & key login, set repositories if necessary)
    - [ ] Macbook
    - [ ] Honor Laptop
- [x] Automate initial setup of client (*input local ansible fact, create directory /opt/ansible set mac address attribute, install community.general from ansible galaxy, install-client, install ansible (ansible-core for dnf) & git, ansible-pull execution*)
    - [x] Input local ansible fact
    - [x] Create /opt/ansible directory
    - [x] Set MAC address attribute
    - [x] Install ipaclient
    - [x] Install ansible & git
    - [x] Execute ansible pull
- [x] Automate initial setup of NFS server
    - [x] Install FreeIPA client
    - [x] Run mount_homes
    - [x] Copy over /opt/templates directory
- [x] Mount remote storage
- [ ] Set up NetBird